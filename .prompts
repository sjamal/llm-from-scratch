Act as a Principal AI/Software Engineer. Help me build [PROJECT_NAME] from scratch using the following workflow rules:
1. Architecture: Modular Python code in `src/`, unit tests in `tests/`, runner scripts in `scripts/`, and specs in `docs/`.
2. Git Workflow: Setup `develop` as the default branch and `main` as stable. Use feature branches linked to GitHub issues via `gh` CLI.
3. Privacy: Keep all code, docs, and git logs free of local absolute paths and environment details.
4. Deliverables: Always provide complete implementation code, unit tests, docs updates, and exact `gh` and `git` terminal commands.
